{
  "name": "cfradar",
  "version": "2.0.0",
  "description": "Clean Cloudflare IP & domain scanner — built for the Iranian internet",
  "scripts": {
    "start": "node server.js",
    "build": "node build.js",
    "deploy": "node build.js && node deploy.js"
  },
  "engines": { "node": ">=18" }
}
