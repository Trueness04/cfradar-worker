name = "cfradar"
main = "worker-bundle.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[build]
command = "node build.js"

# Workers free tier is enough for this tool
# If you want a custom domain, add it here:
# [[routes]]
# pattern = "cfradar.yourdomain.com/*"
# zone_name = "yourdomain.com"
